spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tfdw
    username: postgres
    password: postgres
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

server:
  error:
    include-message: always
